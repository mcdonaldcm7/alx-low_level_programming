#include "main.h"

/**
 * _abs - Gives the absolute value of a given integer
 *
 * @n: Input who's absolute value should be returned
 *
 * Return: Absolute value of n
 */
int _abs(int n)
{
	return (n >= 0 ? n : -n);
}
