#include "main.h"

/**
 * _memset - Sets n number of bytes in s to b
 *
 * @s: Input to be written to
 * @b: character to write
 * @n: number of bytes to write
 *
 * Return: Written string
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
