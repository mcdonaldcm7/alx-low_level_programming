#include "main.h"

/**
 * _memset - Sets index in s to b
 *
 * @s: Input string
 * @b: Input character
 * @n: Input integer (index)
 *
 * Return: Pointer to the modified string
 */
char *_memset(char *s, char b, unsigned int n)
{
	return ((void *) 0);
}
