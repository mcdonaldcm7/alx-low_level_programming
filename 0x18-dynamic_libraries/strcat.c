#include "main.h"

/**
 * _strcat - Concatenates a string
 *
 * @dest: Memory address to add the source string
 * @src: Memory address of source string
 *
 * Return: Pointer to concatenated string
 */
char *_strcat(char *dest, char *src)
{
	return ((void *) 0);
}
