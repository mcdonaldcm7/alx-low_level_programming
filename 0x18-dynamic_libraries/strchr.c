#include "main.h"

/**
 * _strchr - Locates the index of c in s
 *
 * @s: Input string to look through
 * @c: Input character to look for
 *
 * Return: Pointer to c if found, Otherwise NULL
 */
char *_strchr(char *s, char c)
{
	return ((void *) 0);
}
