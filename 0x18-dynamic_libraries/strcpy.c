#include "main.h"

/**
 * _strcpy - Gives a copy of a string
 *
 * @dest: Memory address to copy to
 * @src: Memory address to copy from
 *
 * Return: Returns an address to the string copied
 */
char *_strcpy(char *dest, char *src)
{
	return ((void *) 0);
}
