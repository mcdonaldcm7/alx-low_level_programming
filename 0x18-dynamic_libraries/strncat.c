#include "main.h"

/**
 * _strncat - Concatenates n amount of characters from src to dest
 *
 * @dest: Memory address to concatenate the characters from src
 * @src: Memory address to read the characters to be concatenated
 * @n: Number of characters to read
 *
 * Return: Memory address of the concatenated string
 */
char *_strncat(char *dest, char *src, int n)
{
	return ((void *) 0);
}
