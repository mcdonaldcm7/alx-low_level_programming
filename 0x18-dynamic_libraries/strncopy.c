#include "main.h"

/**
 * _strncpy - Copies specified number of characters from src to dest
 *
 * @dest: Memory address of string to copy to
 * @src: Memory address of string to copy from
 * @n: Number of characters to copy from src to dest
 *
 * Return: Pointer to the concatenated string
 */
char *_strncpy(char *dest, char *src, int n)
{
	return ((void *) 0);
}
