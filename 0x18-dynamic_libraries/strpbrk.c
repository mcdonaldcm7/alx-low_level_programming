#include "main.h"

/**
 * _strpbrk - Finds the first occurence of accept in s if any
 *
 * @s: Input string to look through
 * @accept: Input string to look for
 *
 * Return: Pointer to the character in s if any, Otherwise returns NULL
 */
char *_strpbrk(char *s, char *accept)
{
	return ((void *) 0);
}
