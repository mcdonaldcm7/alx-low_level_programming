#include "main.h"

/**
 * _strspn - Gives the max initial seg of s with characters in accept
 *
 * @s: Input string to look through
 * @accept: Character string to look out for
 *
 * Return: Length of accept in s
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
